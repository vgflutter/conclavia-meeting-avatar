[/Script/PixelStreaming2Settings.PixelStreaming2PluginSettings]
# UE 5.8's EpicRtc validates codec preferences against the complete local and
# remote capability sets. Let WebRTC negotiate the common codec instead of
# forcing a single offer that can be rejected before a browser sees a frame.
WebRTCNegotiateCodecs=True
